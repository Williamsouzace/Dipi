{
  "name": "yes",
  "version": "1.0.0",
  "description": "Automação Dippi",
  "main": "index.js",
  "scripts": {
    "cy:open:prod": "cypress open --config baseUrl=https://app.dippi.com.br --env environment=prod",
    "cy:open:dev": "cypress open --env environment=dev",
    "test:prod": "cypress run --config baseUrl=https://app.dippi.com.br --env environment=prod",
    "test:dev": "cypress run --env environment=dev"
  },
  
  "keywords": [
    "MIT"
  ],
  "author": "William De Souza Pereira",
  "license": "MIT",
  "devDependencies": {
    "@faker-js/faker": "^7.6.0",
    "cypress": "^12.9.0"
  }
}

